# Entry Detail Full Width — Owner-test artifact contract

This document records the distribution evidence boundary for the optional Full Width `gravity_flow.entry_detail` visual variant.

## PR49 implementation status

PR #49 (`Add reversible Full Width Entry Detail visual variant`) is merged. Exact qualified PR Head: `0879d71646c140b2b0b26d00bcad1b5fc749af2b`. Merge commit/current integrated main at the synchronization checkpoint: `272c07f781f8afc9f1c416a935aef67cf200a9b8`.

The required exact-head qualification completed successfully for the exercised repository/pinned-runtime path, including the structural CSS-only gate, Current / Safe preservation and rollback, WU18 browser qualification, Print regressions, WU19/Core Spine/C-D-E-F qualification, WU21, Registration/GF runtime coverage, and the repository release dry-run / exact-ZIP smoke.

The installable ZIP must contain the Full Width production runtime files, including its isolated stylesheet and lifecycle-backed PHP adapters. Development tests, browser fixtures, screenshots, architecture documents, and repository tooling remain excluded by the existing distribution contract.

## Evidence boundary

The successful release dry-run / exact-ZIP smoke is build evidence only. PR49 did not create a new production tag, GitHub Release, publication, or deployment.

Automated/runtime qualification does not create Owner visual acceptance. Final Owner-site Full Width acceptance remains `NOT_PROVEN` until the merged implementation is exercised on the real GeneratePress-hosted Entry Detail page, including responsive layout, native workflow box, Timeline, Print utility, and Current / Safe rollback.
