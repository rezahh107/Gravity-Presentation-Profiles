# WU19 Evidence Lab

This evidence lab exercises the production `PrintDossierPresentationAdapter` through Gravity Flow 3.1.0's native `gravityflow_print_entries` lifecycle.

The browser PDF artifacts are CI-only pagination instrumentation. Production remains HTML/CSS + browser Print.

The lab proves only the pinned synthetic WordPress/Gravity runtime exercised by the workflow; it is not target-production evidence.
